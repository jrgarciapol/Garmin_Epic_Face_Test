# Salida por pantalla. Misma pila que el simulador de conducción: SDL2 pinta
# sobre KMS/DRM, así que en la Raspberry arranca sin escritorio.
pysdl2>=0.9.16
pysdl2-dll>=2.28.0

# Construcción del dibujo (una sola vez, al arrancar) y composición del PNG.
numpy>=1.24
Pillow>=10.0
